# 外れ値を平均値で置き換える関数 (元のコードより)
def replace_outliers_with_mean(df):
    # ... (詳細な処理は省略) ...
    # 外れ値を平均値で置き換える
    
# RR間隔を合計する関数 (元のコードより)
def sum_rr_intervals(df):
    # ... (詳細な処理は省略) ...
    # RR間隔の再計算（特に伊藤さんのデータに使われていた）

# 処理の流れ（元のコードより）
# 1. HeartPyでECGからRR間隔（RR_list）を抽出
# 2. rr_interval_cleaned = replace_outliers_with_mean(rr_interval)
# 3. この cleaned された RR間隔を使って、最終的なHRV解析（おそらくMatlab等）を実行
